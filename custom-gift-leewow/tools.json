{
  "tools": [
    {
      "name": "browse_templates",
      "description": "Fetch customizable product templates. Tool stdout is NDJSON (newline-delimited JSON): one object per line. For each line where type is browse_product, use the `feishu-card-display` skill and execute that line's messageToolCalls immediately with the OpenClaw message tool (do not wait for later lines). After the line where type is browse_complete, reply NO_REPLY. If a line has type browse_error, surface the error to the user. Python emits each browse_product as soon as that product's display payload is ready so cards can appear progressively. Single-object JSON payloads are not used for this tool.",
      "executor": "bash",
      "command": "python3 scripts/browse.py --category '{{category}}' --count {{count}} --channel '{{channel}}' --json --stream",
      "parameters": {
        "type": "object",
        "properties": {
          "category": {
            "type": "string",
            "description": "Optional category filter (e.g. 'bag', 'accessory', 'home', 'apparel'). Leave empty to browse all.",
            "default": ""
          },
          "count": {
            "type": "integer",
            "description": "Number of products/messages to return (1-10, default 5)",
            "default": 5,
            "minimum": 1,
            "maximum": 10
          },
          "channel": {
            "type": "string",
            "description": "Rendering channel. Use 'feishu' for current production flow. Other channels are reserved for future renderers and should not be guessed by the agent.",
            "default": "feishu"
          }
        }
      }
    },
    {
      "name": "generate_preview",
      "description": "Upload a design image and start generation. Returns task state only. Then call get_generation_status(task_id, poll=true) to get the preview image path plus the final reply markdown. Do NOT fabricate or guess any URL — only use purchaseUrl/replyMarkdown from get_generation_status.",
      "executor": "bash",
      "command": "python3 scripts/generate.py --image-path '{{image_path}}' --template-id {{template_id}} --design-theme '{{design_theme}}' --aspect-ratio '{{aspect_ratio}}' --json",
      "parameters": {
        "type": "object",
        "properties": {
          "image_path": {
            "type": "string",
            "description": "Local file path of the design image. MUST be in workspace directory: ~/.openclaw/workspace/"
          },
          "template_id": {
            "type": "integer",
            "description": "Product template ID (from browse_templates results)"
          },
          "design_theme": {
            "type": "string",
            "description": "Design theme or style description (e.g. 'cute cartoon', 'minimalist')",
            "default": ""
          },
          "aspect_ratio": {
            "type": "string",
            "description": "Aspect ratio: '3:4', '1:1', '4:3'",
            "default": "3:4"
          }
        },
        "required": ["image_path", "template_id"]
      }
    },
    {
      "name": "get_generation_status",
      "description": "Check generation task status. Returns JSON with displaySkill=`feishu-card-display`, messageToolCalls, replyMarkdown and, when completed, localImagePath plus purchaseUrl. Use the `feishu-card-display` skill to execute messageToolCalls exactly, in order, in the current conversation; then reply with NO_REPLY.",
      "executor": "bash",
      "command": "python3 scripts/get_status.py '{{task_id}}' {{#if poll}}--poll{{/if}} --timeout {{timeout}} --json",
      "parameters": {
        "type": "object",
        "properties": {
          "task_id": {
            "type": "string",
            "description": "Task ID returned by generate_preview"
          },
          "poll": {
            "type": "boolean",
            "description": "Wait and poll until generation completes",
            "default": true
          },
          "timeout": {
            "type": "integer",
            "description": "Maximum wait time in seconds",
            "default": 120
          }
        },
        "required": ["task_id"]
      }
    },
    {
      "name": "cos_presign_url",
      "description": "Generate a presigned URL for accessing COS (Tencent Cloud Object Storage) images. Useful for private buckets or when images need temporary access tokens.",
      "executor": "bash",
      "command": "python3 scripts/cos_presign.py '{{cos_url}}' --expired {{expired}} --json",
      "parameters": {
        "type": "object",
        "properties": {
          "cos_url": {
            "type": "string",
            "description": "COS URL to generate presigned URL for (e.g., https://bucket.cos.region.myqcloud.com/key.png)"
          },
          "expired": {
            "type": "integer",
            "description": "URL expiration time in seconds (default: 3600 = 1 hour)",
            "default": 3600
          }
        },
        "required": ["cos_url"]
      }
    }
  ]
}
